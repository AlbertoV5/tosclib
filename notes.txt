Manual Release

Update version number in:
    - tosclib/setup.py
    - docs/source/conf.py release, version

Development:

python -m pip install -e .

docs\make html

Release:

python -m build

twine upload dist/*


Virtual Envs:

py -3.8 -m venv venv\38


TO DO:

- Cover missing enums.
- Make parser and matcher.

